name: Deploy Mini Apps to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            apps/team-randomizer/package-lock.json
            apps/cosmic-draw-arena/package-lock.json
            apps/ai-analysis-skillset/package-lock.json

      - name: Install team-randomizer dependencies
        working-directory: apps/team-randomizer
        run: npm ci

      - name: Install cosmic-draw-arena dependencies
        working-directory: apps/cosmic-draw-arena
        run: npm ci

      - name: Install ai-analysis-skillset dependencies
        working-directory: apps/ai-analysis-skillset
        run: npm ci

      - name: Build team-randomizer
        working-directory: apps/team-randomizer
        run: npm run build

      - name: Build cosmic-draw-arena
        working-directory: apps/cosmic-draw-arena
        run: npm run build

      - name: Build ai-analysis-skillset
        working-directory: apps/ai-analysis-skillset
        run: npm run build

      - name: Collect site files
        run: |
          mkdir -p public/team-randomizer
          cp -R apps/team-randomizer/dist/. public/team-randomizer/
          mkdir -p public/cosmic-draw-arena
          cp -R apps/cosmic-draw-arena/dist/. public/cosmic-draw-arena/
          mkdir -p public/ai-analysis-skillset
          cp -R apps/ai-analysis-skillset/dist/. public/ai-analysis-skillset/

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
